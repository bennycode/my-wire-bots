{
  "dependencies": {
    "@types/node": "9.4.6",
    "@wireapp/core": "0.0.32",
    "dotenv": "5.0.0",
    "typescript": "2.7.1"
  },
  "devDependencies": {
    "rimraf": "2.6.2"
  },
  "license": "UNLICENSED",
  "main": "index.js",
  "name": "my-wire-bots",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bennyn/my-wire-bots.git"
  },
  "scripts": {
    "build": "yarn build:node",
    "build:node": "tsc",
    "clear": "rimraf dist",
    "dev": "yarn dist && yarn start",
    "dist": "yarn clear && yarn build",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "version": "1.0.0"
}
